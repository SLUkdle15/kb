# Build a Chat System Over This Vault

## Idea

Build a chat interface on top of this knowledge vault, so its notes can be queried and explored conversationally (e.g. RAG-style over the Markdown notes).

## If Promoted to a Project

- Clarify the outcome: personal assistant over the vault? search tool? something shareable?
- Decide the approach (local LLM vs API, embedding/indexing the notes, chat UI vs CLI).
