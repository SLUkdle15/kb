# Reorganize the Resources Folder

## Outcome

Four `resources` folders — `software-engineering`, `personal-knowledge-management`, `emotional-regulation`, and `behavior-change` — are easy to navigate: notes are findable by subtopic, nothing sits miscategorized at a folder root, and any subfolder that has grown too big is split.

`software-engineering` is the concrete pain point: 30+ notes, with `system-architecture/` alone holding ~20 flat notes (mostly DDIA reading notes on storage engines, replication, partitioning, transactions, distributed systems) with no further grouping, plus one loose note (*2026-05-28 - JavaScript Dev to Main Merge Review*) sitting directly in the `software-engineering` root instead of any subfolder. The other three folders are small (4-8 notes each, all flat) and may only need a light check rather than a real restructure.

## Deadline

None — this is upkeep, not time-bound.

## Definition of Done

- `software-engineering/system-architecture` is split into logical subfolders (e.g. by DDIA part or topic — storage engines, replication and partitioning, transactions and consistency, distributed systems), and the loose root-level note is filed into the right place.
- `personal-knowledge-management`, `emotional-regulation`, and `behavior-change` are each reviewed and either confirmed fine as flat folders or lightly reorganized.
- No orphaned or miscategorized notes remain at any of the four folder roots.

## Next Actions

- [[next/next-actions/2026-08-29 - Propose a Subfolder Structure for System Architecture]]

## Notes

Scope and next action confirmed with the user on 2026-08-29 via the `next` and `project` skills — started as a vague "refactor resources in kb" capture, narrowed to these four folders once asked.
